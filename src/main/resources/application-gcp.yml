server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://${CLOUD_SQL_HOST:136.85.104.221}:3306/user-service?useSSL=true&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:free-trial-first-project}
    password: ${DB_PASSWORD}

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/

jdbc:mysql://136:
  85:
    104:
      221:3306/user-service?useSSL=true&allowPublicKeyRetrieval=true: